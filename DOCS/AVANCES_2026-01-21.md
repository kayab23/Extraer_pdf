AVANCES (2026-01-21)

Resumen de acciones realizadas:

- Se implementó extracción posicional usando `pdfplumber` para mejorar captura de tablas y campos multilínea.
- Se añadieron y ajustaron funciones de limpieza: `clean_id`, `clean_nombre`, `extract_id_from_nombre`.
- Se implementó `extraer_partidas_por_posicion` para reconstruir partidas multilínea (SEC, FRACCION, DESCRIPCION, TASA_IGI).
- Se añadió post-procesamiento para extraer `IGI` incrustado en `DESCRIPCION` hacia `TASA_IGI` y limpiar la `DESCRIPCION`.
- Se aplicaron heurísticas para reconstruir `ID_FISCAL` cuando queda truncado en el PDF.
- Se generó y validó `salida/pedimentos_completo.csv` y se revisaron casos con `FRACCION=90192001`.

Archivos eliminados (temporales):

- scripts/check_fraccion.py
- scripts/find_description.py
- scripts/print_fracc.py

Recomendaciones y siguientes pasos:

- Añadir `.gitignore` para excluir `.venv/` y `__pycache__/`.
- Definir conjunto de tests con PDFs representativos y resultados esperados.
- Revisar manualmente filas atípicas en `salida/pedimentos_completo.csv`.

Contacto: cambios realizados por el equipo de extracción de datos.
